{% extends 'catalog/index.html.twig' %}

{% block content %}
    <div class="product-list">
        {% for product in category.products %}
            <div class="row product-item">
                <div class="col-sm-3">
                    <div class="thumbnail product-images">
                        <a href="{{ path('catalog_index', {'fullPath': product.fullPath}) }}">
                            <img src="{{ product.uploadPath }}{{ product.image }}"
                                 alt="{{ product.name }}">
                        </a>
                    </div>
                </div>
                <div class="col-sm-9">
                    <h2 class="h4">
                        <a href="{{ path('catalog_index', {'fullPath': product.fullPath}) }}">{{ product.sku }}</a>
                        <small>{{ product.name }}</small>
                    </h2>
                    {#{{ include('@catalog/components/_product_property.twig') }}#}
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}