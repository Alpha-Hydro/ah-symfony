{% extends 'admin.html.twig' %}

{% set pageTitle = product.sku %}

{% block title %}{{pageTitle}} - Edit Products{% endblock %}

{% block pageHeader %}
    {% spaceless %}
    <h1 class="display-4 mb-4">{{ pageTitle}}<small class="text-muted h4 font-weight-light d-block">{{ product.name }}</small></h1>
    {% endspaceless %}
{% endblock %}

{% block body %}
    {{ include('admin/products/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('products_index') }}">back to list</a>

    {{ include('admin/products/_delete_form.html.twig') }}
{% endblock %}
