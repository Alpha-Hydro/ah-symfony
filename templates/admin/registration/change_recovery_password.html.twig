{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block body %}
<div class="row">
    <div class="col-md-6 col-md-offset-3">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class=" form-group ">
            <div class="text-right">
                <button type="submit" class="btn btn-default btn-success">{{ label_button | default('Сохранить') }}</button>
                <a href="{{ path('index') }}" type="reset" class="btn btn-default">Отмена</a>
            </div>
        </div>
    {{ form_end(form) }}
    {{ include('admin/registration/_flash_eeror_message.html.twig') }}
    </div>
</div>
{% endblock %}

{% block header %}
    {{ include('admin/components/_logo_ru_svg.html.twig')}}
{% endblock %}

